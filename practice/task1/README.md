Исходные данные:
- В репозитории есть директория data/
- Внутри неё находится ~100 .txt файлов
- В одном из файлов спрятан SECRET_KEY
- Также в репозитории есть скрипт check_key.sh

Что нужно сделать:
- Найти файл, в котором хранится SECRET_KEY
- Узнать значение этого ключа
- Задать ключ в переменную окружения
- Запустить скрипт так, чтобы он выполнился успешно

Подсказки:\
```ls``` ```grep``` ```cat``` ```export```\
Можно почитать какие аргументы и флаги в grep:
```bash
grep --help
```